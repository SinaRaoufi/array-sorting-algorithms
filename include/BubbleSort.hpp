#ifndef BUBBLE_SORT_IG
#define BUBBLE_SORT_IG

#include <iostream>

void notOptimizedBubbleSort(int[], size_t);
void optimizedBubbleSort(int[], size_t);
void mostOptimizedBubbleSort(int[], size_t);

#endif